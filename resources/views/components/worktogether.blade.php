<section class="worktogether" id="worktogether">
    <div class="container worktogether-content">
        <div class="worktogether-image">
            <img src="{{ $imageSrc }}" alt="Whitespace worktogether">
        </div>
        <div class="worktogether-text">
            <h2>{{ $title }}</h2>
            <p>{{ $description }}</p>
            <a href="{{ $buttonLink }}" class="btn-primary">Get Started <i class="fa-regular fa-arrow-right"></i></a>
        </div>
    </div>
</section>
